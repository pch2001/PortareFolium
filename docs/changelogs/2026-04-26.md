# Changelog: 2026-04-26

## v0.12.104

### fix: SQLite refuge admin 자동 활성화를 local-only로 제한

- `src/lib/local-sqlite-refuge-admin.ts` 신규: `SQLITE_REFUGE_ADMIN_BYPASS=local-dev-only`, localhost host, active SQLite refuge mode, non-Vercel runtime을 모두 만족할 때만 fake admin session을 발급
- `src/lib/server-admin.ts`, admin page/login/API routes/public PDF auth checks: 직접 `auth()` 호출 대신 effective admin session helper를 경유해 Auth.js `MissingSecret` 없이 local refuge admin을 유지
- `src/lib/admin-credentials.ts`, `src/components/AuthSessionProvider.tsx`, `src/app/layout.tsx`: local refuge 전용 Auth.js fallback secret과 initial SessionProvider session을 추가하되 Vercel Preview/Production에서는 비활성
- `src/proxy.ts`: explicit local refuge opt-in + localhost 요청은 no-cookie admin/API 요청을 통과시키고, 실제 권한은 server-side gate에서 다시 확인
- `.env.example`, `docs/SQLITE_REFUGE.md`, `.agents/directives/05-architecture.md`, `.agents/directives/06-security.md`: local-only bypass 사용법과 Vercel 등록 금지 원칙 문서화
- `src/__tests__/local-sqlite-refuge-admin.test.ts`: Vercel/production/preview host에서 bypass가 열리지 않는 회귀 테스트 추가
- `package.json`: patch version `0.12.104`로 증가

## v0.12.103

### docs: SQLite refuge 전환 운영 가이드 추가

- `docs/SQLITE_REFUGE.md` 신규: Supabase → SQLite refuge 진입 절차, SQLite refuge → Supabase 복귀 절차, dry-run/apply 체크포인트, R2 asset 기능 유지 원칙 정리
- `package.json`: patch version `0.12.103`으로 증가

## v0.12.102

### feat: SQLite refuge 운영 명령과 로컬 Supabase 대체 경로 추가

- `package.json`: `refuge:pull`, `refuge:push`, `refuge:deactivate` 스크립트 추가. Supabase 중단 시 백업 JSON → 로컬 SQLite refuge 활성화 → Supabase 복귀 후 guarded replay 흐름을 CLI로 실행 가능하게 함
- `scripts/refuge-pull.ts`, `scripts/refuge-push.ts`, `scripts/refuge-deactivate.ts`: 백업/Supabase source에서 refuge DB 생성, mode 전환, journal 기반 dry-run/apply replay 구현
- `src/lib/refuge/*`, `src/lib/supabase.ts`: serverClient를 SQLite refuge mode로 라우팅하고 supported table read/write, mutation journal, drift/conflict guard를 제공
- `src/app/admin/actions/*`, `src/components/admin/*`: refuge mode에서 unsupported DB mutation surface를 숨기거나 서버 경계에서 차단. R2 기반 image upload 및 storage move/delete는 core asset feature라 계속 허용
- `src/__tests__/refuge-client.test.ts`, `src/__tests__/upload-image-route.test.ts`, `src/__tests__/storage-ops-route.test.ts`: refuge client 정책, refuge 중 R2 upload/list/move/delete 허용 회귀 테스트 추가
- `package.json`: patch version `0.12.102`로 증가

## v0.12.101

### chore: middleware → proxy 파일 컨벤션 마이그레이션 (Next.js 16)

- `src/middleware.ts` → `src/proxy.ts` `git mv` 후 export 함수명 `middleware` → `proxy` 로 변경 (Next.js 16 deprecation 경고 해소). config matcher 동일 유지
- `.agents/directives/06-security.md`: "Routes" 섹션 + "Lessons learned" 11/12 항목의 `src/middleware.ts` 참조와 "Middleware" 명칭을 `src/proxy.ts` / "Proxy" 로 갱신
- `package.json`: patch version `0.12.101`로 증가

## v0.12.100

### refactor: shared IP 헬퍼 통합과 admin panel any/console.error 정리

- `src/lib/request-ip.ts` 신규: `getRequestIpFromHeaders(headers: Headers)` — `x-forwarded-for` → `x-real-ip` → `"unknown"` 순 추출. NextAuth Node `Headers` + NextRequest Web `Headers` 양쪽에서 호출 가능
- `src/auth.ts`, `src/app/api/mcp/route.ts`: 인라인 IP 추출 코드를 `getRequestIpFromHeaders` 호출로 통합 (중복 제거)
- `src/app/admin/actions/public-data.ts`: `searchPublicContent` 의 `postQuery`/`portfolioQuery` 를 `let` 바인딩 + `.or()` 재할당으로 변경 — supabase-js builder mutation 의존 제거
- `src/app/admin/actions/snapshots.ts`: `createSnapshot` error envelope 을 `{ success: false; error: string }` 으로 통일 — server action 응답 형식 일관성
- `src/middleware.ts`: 코멘트를 "defense-in-depth" → "unauthenticated 트래픽 필터" 로 정직하게 수정. 실제 admin 권한은 `requireAdminSession()` 이 담당함을 명시
- `src/lib/r2-path-policy.ts`: `matchesAllowedPrefix` 의 `value === p` 분기가 key path 에서 dead branch 임을 코멘트로 명시
- `src/components/admin/panels/ResumePanel.tsx`: `useState<any>` → `useState<Resume | null>`, `legacy as any` → `Record<string, unknown>`, `catch (e: any)` → `catch (e)` + `instanceof Error` 가드, `onEmojiSelect (e: any)` → `(e: { native: string })`. `autoSave` silent catch 에 `console.error("[ResumePanel::autoSave] ...")` 추가
- `src/components/admin/panels/AboutPanel.tsx`: `useState<any>` → `useState<Record<string, unknown> | null>`, `eslint-disable` 코멘트 제거
- `src/components/admin/skills/SkillsAdminSection.tsx`: `onEmojiSelect (e: any)` → `(e: { native: string })`
- `src/components/admin/panels/PromptLibraryPanel.tsx`: `console.error("Failed to copy:", err)` → `console.error("[PromptLibraryPanel::handleCopy]", err)` / `[PromptLibraryPanel::handlePortfolioCopy]` — project log format 정렬
- `package.json`: patch version `0.12.100`로 증가

## v0.12.99

### fix: admin dashboard inactivity 가드와 로그인/모달 a11y 보강

- `src/components/admin/AdminDashboard.tsx`: `visibilitychange` 리스너 추가로 background tab 에서 inactivity timer 정지 + 활성화 시 lastActivity 리셋. logout 1분 전 `window.confirm` 사전 경고 추가 — `Date.now()` 점프로 인한 silent mid-edit logout 차단. `PANELS_OWN_HEIGHT` Set 모듈 레벨 추출로 9-tab ternary 중복 제거
- `src/components/admin/LoginForm.tsx`: email/password input 에 `id` + `htmlFor` paired sr-only label + `autoComplete="email" | "current-password"` 추가 — screen reader 안내 + password manager 자동 채움 활성
- `src/components/admin/EditorStatePreservation.tsx`: 4개 inline confirm 모달에 `role="alertdialog"` + `aria-modal="true"` + `aria-describedby` + 매칭 warning text `id` 추가 — 색상 only destructive signal 보강
- `package.json`: patch version `0.12.99`로 증가

## v0.12.98

### fix: server action bootstrap error 로깅과 reorder partial-write 감소

- `src/app/admin/actions/about.ts`, `posts.ts`, `portfolio.ts`: bootstrap 함수의 `Promise.all` 결과에서 per-query `error`를 destructure 해 `console.error("[<file>::<func>] ${msg}")` 로 기록 — 일부 query 실패 시 빈 array bootstrap 대신 원인이 로그에 남도록
- `src/app/admin/actions/portfolio.ts`: `reorderFeaturedPortfolioItems`, `batchSetPortfolioJobField` — 순차 `for` 루프를 bulk slug `select(...).in("id", ids)` + `Promise.all` parallel update + `Promise.all(slugs.map(revalidate))` 로 전환. partial-write 가능성 줄이고 round-trip 단축
- `src/app/admin/actions/posts.ts`: `batchSetPostPublished`, `batchSetPostJobField` — revalidate 순차 await 를 `Promise.all` 로 전환
- `package.json`: patch version `0.12.98`로 증가 (changelog 누락분 v0.12.99 commit 에 흡수)

## v0.12.97

### chore: docs root 정리 — security/test 직책 directive 흡수, 잉여 가이드 삭제

- `.agents/directives/06-security.md` 신규: trust 경계, MDX trust 모델, R2/route guard-rails, 본 branch 보안 수정에서 도출된 24개 lesson learned 정리. 신규 인증/저장 코드 작성 시 강제 follow.
- `.agents/directives/04-workflow.md`: Testing Gate 섹션에 도구 명령 표 + Playwright 프로젝트/인증 흐름 + 신규 E2E 추가 가이드 흡수.
- `AGENTS.md`: manifest 에 `06-security.md` ref 추가.
- 삭제: `docs/IMAGE_ORPHAN_CLEANUP.md` (코드 + 테스트가 source of truth), `docs/MCP_GUIDE.md` (외부 caller 가이드, 핵심 정보는 `05-architecture.md` 에 condensed), `docs/SECURITY.md` (06-security.md 로 이전), `docs/SEO.md` (일회성 setup tutorial), `docs/TEST.md` (04-workflow.md 로 흡수).
- `package.json`: patch version `0.12.97`로 증가

## v0.12.96

### chore: root cleanup — vendor 분리 + plans/pr/TODO 위치 정리

- `vendor/keditor-0.7.21/` 신설 (← `KEDITOR-0.7.21/`)
- `.agents/prompts/` 신설 (← `prompt-templates/`)
- `.agents/prompt-assets/` 신설 (← `prompt/`, gitignored)
- `docs/plans/active/`, `docs/plans/archive/`, `docs/pr/` 신설 + 각 폴더 `README.md` (gitignored 폴더의 의미 안내)
- root 의 모든 `PLAN_*.md`, `PR_*.md`, `TODO.md`, `USER_TASKS.md` → `docs/plans/{active,archive}/`, `docs/pr/`, `docs/TODO.md`, `docs/USER_TASKS.md` 로 이동
- `PLAN_SECURITY.md` 삭제 — v0.12.87~v0.12.94 commit 으로 모든 항목 해소
- `PR_PROVIDER_API.md` 삭제 — 현 사용량 대비 ROI 낮음
- `.gitignore`: `prompt/`, `KEDITOR-0.7.21/`, `PROMPT_*.md` 룰 제거 + `vendor/`, `docs/plans/`, `docs/pr/`, `docs/TODO.md`, `docs/USER_TASKS.md`, `.agents/prompt-assets/` 룰 추가 + `docs/plans/README.md`, `docs/pr/README.md` 화이트리스트
- `AGENTS.md`: "File locations" 표 추가 — root `.md` 화이트리스트 + plan/PR/TODO 위치 명시
- `.agents/directives/01-behavior.md`: `TODO.md` → `docs/TODO.md`, `USER_TASKS.md` → `docs/USER_TASKS.md`
- `.agents/directives/04-workflow.md`: PR 본문 위치 → `docs/pr/<branch>.md`, plan 위치 → `docs/plans/{active,archive}/<slug>.md`, `PLAN_` prefix 폐기
- `.agents/directives/05-architecture.md`: "File location conventions" 표 추가 (상세)
- `package.json`: patch version `0.12.96`로 증가

## v0.12.95

### feat: shared agent directive 폴더 .agents/ 도입

- `.agents/directives/` 신규: 사용자 custom directive (`01-behavior.md` ~ `05-architecture.md`) — Claude Code / Codex / Gemini 가 모두 참조하는 canonical 위치
- `.agents/directives/omc/` 신규: `.claude/rules/` 의 commit-tracked snapshot — OMC plugin 갱신 분 자동 흡수
- `scripts/sync-omc-directives.mjs` 신규: `.claude/rules/*.md` → `.agents/directives/omc/*.md` 일방향 sync
- `package.json`: `postinstall` + `sync-omc` 스크립트 추가 — 설치 직후 자동 sync
- `.husky/pre-commit`: sync 실행 + `.agents/directives/omc` 자동 stage 단계 추가
- `.gitignore`: `.claude/rules/` 등록, `GEMINI.md` whitelist 추가
- `AGENTS.md`: 새 `.agents/` 경로를 가리키는 manifest 형태로 refactor
- `CLAUDE.md`, `GEMINI.md`: AGENTS.md 로 redirect 하는 1줄 파일
- 기존 untracked `agents/` 폴더는 `.agents/directives/` 로 이동
- `package.json`: patch version `0.12.95`로 증가

## v0.12.94

### docs: SECURITY 가이드 추가

- `docs/SECURITY.md` 신규: trust 경계 (admin/MCP/public), MDX content trust 모델, R2 path policy, route middleware gate, deployment 가정, secret/token rotation 절차 정리
- `package.json`: patch version `0.12.94`로 증가

## v0.12.93

### feat: admin route middleware gate

- `src/middleware.ts` 신규: NextAuth session cookie 부재 시 `/admin/(?!login)` + `/api/upload-image|storage-ops|run-migrations` 요청을 401/redirect로 차단
- 실제 권한 검증은 기존 `requireAdminSession()` / `isAdminSession()`에서 수행하며 middleware는 unauthenticated 요청을 빠르게 차단하는 defense-in-depth layer
- `package.json`: patch version `0.12.93`로 증가

## v0.12.92

### fix: MCP 엔드포인트 인증 및 invalid token throttle

- `src/lib/mcp-rate-limit.ts` 신규: in-memory IP 기반 invalid attempt 카운터 — 5분 window 내 30회 초과 시 5분 차단
- `src/app/api/mcp/route.ts`: GET 응답도 Bearer 인증 필수, 401/429 응답을 JSON-RPC 에러로 통일하고 invalid token 시도를 IP 기반으로 throttle
- `src/__tests__/mcp-rate-limit.test.ts`: 차단/리셋 회귀 테스트 추가
- `package.json`: patch version `0.12.92`로 증가

## v0.12.91

### chore: AUTH_SECRET 컨벤션 정렬 + trustHost 운영 노트

- `src/lib/admin-credentials.ts`: `getAuthSecret()` 신규 — `AUTH_SECRET` 우선, 없으면 `NEXTAUTH_SECRET` fallback. setup 검사도 `AUTH_SECRET`을 누락 키로 보고
- `src/auth.ts`: NextAuth `secret`을 `getAuthSecret()`로 주입해 두 키 모두 지원
- `src/components/admin/LoginForm.tsx`, `src/__tests__/admin-credentials.test.ts`, `src/__tests__/login-form-guide.test.tsx`: env 키 라벨을 `AUTH_SECRET`으로 갱신
- `.env.example`: `AUTH_SECRET` 권장 + `NEXTAUTH_SECRET`이 backward-compatible fallback임을 명시
- `USER_TASKS.md`: `AUTH_SECRET` rename 가이드, MCP token rotation, trustHost 운영 가정 추가
- `package.json`: patch version `0.12.91`로 증가

## v0.12.90

### fix: nextauth signIn 검증 강화 + LIKE escape + agent token 필터

- `src/auth.ts`: `signIn` callback에서 provider 외에 `isAdminEmail(user?.email)`까지 검증해 다른 경로로 user 객체가 만들어지더라도 admin 권한 부여 차단
- `src/app/admin/actions/public-data.ts`: 공개 검색 query에 `escapeLikePattern`을 적용해 사용자가 `%`/`_`/`\\`로 ilike wildcard 매칭을 우회하지 못하도록 차단
- `src/app/admin/login/page.tsx`: site_name 값이 broken JSON일 때 `JSON.parse` 예외로 로그인 페이지가 500이 되지 않도록 try/catch 처리
- `src/lib/agent-token.ts`: SQL 단계에서 `revoked = false` + `expires_at > now()` 필터 적용 + `last_used_at` 60초 throttle로 invalid/유효 토큰 모두에 대한 DB write 증폭 차단
- `package.json`: patch version `0.12.90`로 증가

## v0.12.89

### fix: admin 로그인 rate limit DB 부재 시 fail-closed

- `src/lib/admin-login-rate-limit.ts`: in-memory fallback 제거 — Supabase store가 없거나 조회 실패하면 `store-unavailable` 사유로 차단 상태 반환
- `src/auth.ts`: rate limit 조회가 fail-closed로 차단되면 그대로 로그인 거부 (다중 instance에서 분산 brute force 회피 차단)
- `src/__tests__/admin-login-rate-limit.test.ts`: in-memory store mock으로 정상 흐름 검증 + serverClient 없을 때 fail-closed 회귀 테스트 추가
- `package.json`: patch version `0.12.89`로 증가

## v0.12.88

### fix: NEXTAUTH_SECRET fallback 제거 + admin 로그인 setup gate

- `src/auth.ts`: dev 환경 hard-coded secret fallback을 제거하고 `NEXTAUTH_SECRET` 미설정 시 NextAuth가 부팅 단계에서 실패하도록 변경
- `src/auth.ts`: `authorize()` 진입부에서 `isAdminCredentialSetupComplete()` 검사 후 env 미완료면 rate limit counter 소모 없이 즉시 null 반환
- `package.json`: patch version `0.12.88`로 증가

## v0.12.87

### fix: R2 storage 경로 검증 강화

- `src/lib/r2-path-policy.ts` 신규: content prefix allowlist (`portfolio/`, `blog/`, `books/`, `about/`, `resume/`, `misc/`) + segment charset/traversal 검증 + 이미지 확장자 → MIME 결정 + 업로드 size cap 정의
- `src/app/api/upload-image/route.ts`: path를 `assertSafeR2Key`로 검증하고 client `file.type` 대신 확장자 기반 ContentType 사용, `MAX_UPLOAD_BYTES` 초과 시 413 반환
- `src/app/api/storage-ops/route.ts`: list/move/delete prefix와 delete-keys 항목을 policy 헬퍼로 검증해 빈 prefix·traversal·non-allowlist 경로 차단
- `src/__tests__/r2-path-policy.test.ts`: allowlist/거부 케이스 회귀 테스트 추가
- `package.json`: patch version `0.12.87`로 증가
